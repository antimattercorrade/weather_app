## Weather App

A React Native app to show weather conditions. It supports both Android and iOS.

### Configuration

1) Replace the API_KEY in the "Home.js" file to your Open Weather Map API Key.
2) Install Android Studio and configure it.
3) Run `npx react-native run-android` or `npx react-native run-ios`
